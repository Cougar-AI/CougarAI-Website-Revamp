[pytest]
asyncio_mode = strict
asyncio_default_fixture_loop_scope = function
asyncio_default_test_loop_scope = function
pythonpath = .
testpaths = tests
markers =
    unit: Pure unit tests — no Docker or database required
    integration: Integration tests — requires Docker PostgreSQL
addopts = --strict-markers